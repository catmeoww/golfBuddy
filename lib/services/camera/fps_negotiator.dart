// TODO: LLD §3 — query availableFrameRateRanges, walk 240→120→60→30 ladder.
class FpsNegotiator {
  const FpsNegotiator();

  Future<int> negotiate() async {
    throw UnimplementedError('FpsNegotiator.negotiate');
  }
}
