// TODO: LLD §4 — stitch video service + pose pipeline + metric calculators.
import '../models/swing_analysis.dart';

class AnalyzeSwing {
  const AnalyzeSwing();

  Future<SwingAnalysis> call(String videoPath) async {
    throw UnimplementedError('AnalyzeSwing');
  }
}
