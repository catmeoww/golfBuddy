// TODO: LLD §8 — produce MetricDiff list for comparison screen.
class CompareSwings {
  const CompareSwings();

  Future<void> call(String sessionIdA, String sessionIdB) async {
    throw UnimplementedError('CompareSwings');
  }
}
