# iOS

The full iOS Runner Xcode project is generated by `flutter create . --platforms=ios`
when Flutter tooling is available locally. Only the canonical source files
(`Info.plist`, `AppDelegate.swift`) are hand-committed here. Run the command
above before first build to materialize the Xcode project, Pods, and schemes.
